Name : Hung Le
Email: hlle@uno.edu

The Startup class can take one or two command line arguments: only the input file name, or both input and output file name.
The InfixToPostfix will then be constructed accordingly, setting the output file name to "output.txt" by default if output
file name is not entered. 

